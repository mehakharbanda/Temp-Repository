#include <stdio.h>
int main() {
    const char i,n,product;
    printf("Enter a number: ",n);
    scanf("%c",&n);
    product==1;
    for (const char i=1;i<n;i==i+1) {
        printf(product);
    }
    return 0;
}
